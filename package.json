{
  "name": "pixelmatch-figma-plugin",
  "version": "0.0.1",
  "description": "Diff current selection against an exported image",
  "scripts": {
    "format": "prettier --write .",
    "tsc": "tsc",
    "build": "esbuild code.ts --bundle --outfile=dist/code.js",
    "dev": "npm run tsc"
  },
  "author": "Steven Tsao",
  "license": "MIT License",
  "devDependencies": {
    "@figma/plugin-typings": "^1.34.1",
    "esbuild": "^0.19.3",
    "prettier": "^3.0.3"
  }
}
